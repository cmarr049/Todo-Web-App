const task = document.querySelector('#input');
const container = document.querySelector('.container');

function addTask() {
  if (task === '') {
    alert('Go find something to do!');
  } else {
    let div = document.createElement('div');
  }
}
